import { PlaceholderPage } from "@/components/app/placeholder-page";

export const dynamic = "force-dynamic";

export default function ExplorePage() {
  return (
    <PlaceholderPage
      eyebrow="Explore"
      title="Full catalog browsing lands next"
      body="The home screen already surfaces real featured catalog titles and add-to-library flows. This route is the next dedicated browser, but for this phase it stays intentionally light."
    />
  );
}
