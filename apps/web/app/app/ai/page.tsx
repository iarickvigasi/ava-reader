import { PlaceholderPage } from "@/components/app/placeholder-page";

export const dynamic = "force-dynamic";

export default function AiPage() {
  return (
    <PlaceholderPage
      eyebrow="AVA AI"
      title="Your reading copilot is queued up"
      body="The authenticated shell and home systems are real now, but the AI workspace is intentionally deferred. This route reserves the space without pretending the product exists yet."
    />
  );
}
