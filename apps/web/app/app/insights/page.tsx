import { PlaceholderPage } from "@/components/app/placeholder/placeholder-page";

export const dynamic = "force-dynamic";

export default function InsightsPage() {
  return (
    <PlaceholderPage
      eyebrow="Insights"
      title="Insights will deepen after the reader ships"
      body="Daily mastery, reading totals, and recent annotations are already powering the real home screen. This route is the future dedicated analytics surface."
    />
  );
}
